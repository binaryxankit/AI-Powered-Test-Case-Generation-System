[pytest]
testpaths = backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers --color=yes
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: tests that exercise the database or external services.
asyncio_mode = auto
